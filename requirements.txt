# Sovereign Citadel — Python runtime + test dependencies.
# Pinned to the versions this repo is built and tested against.
# Local-first: nothing here phones home; cloud egress is gated by the router.

# --- Web / API (local loopback server on 127.0.0.1:7734) ---
fastapi==0.115.8
uvicorn[standard]==0.34.0
pydantic==2.12.5
python-dotenv==1.0.1

# --- HTTP clients (local Ollama + tier-gated cloud routing) ---
httpx==0.28.1
requests==2.32.5

# --- Retrieval engine (chunker token counting) ---
tiktoken==0.8.0

# --- Local cross-encoder reranker (bge-reranker-base; pulls torch + transformers) ---
sentence-transformers==5.3.0

# --- Drop-in file conversion -> markdown, local-only (no cloud, enable_plugins=False) ---
markitdown[pdf,docx,pptx,xlsx]==0.1.6

# --- Vault file watcher (Obsidian MCP bridge) ---
watchdog==6.0.0

# --- Connectors: Gmail + Calendar (optional features; scripts/fetch_*.py) ---
google-auth==2.48.0
google-auth-oauthlib==1.3.0
google-api-python-client==2.188.0

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.3
pytest-timeout==2.4.0
