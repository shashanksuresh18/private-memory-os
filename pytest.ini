[pytest]
testpaths = tests
addopts = -ra -q --strict-markers
markers =
    integration: hits live local services (Ollama, OPF). Skip by default.
    gating: load-bearing security/correctness invariants. Non-bypassable.
